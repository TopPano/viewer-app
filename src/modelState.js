// Add an object to current state.
TOPPANO.addCurrentState = function(id, prop) {
    TOPPANO.ui.currentState[id] = prop;
};

// Modify the state. (Create/update/delete an object)
TOPPANO.modifyState = function(id, type, action, prop) {
    // TODO: modify diffState.
};

