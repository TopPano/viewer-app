var test = TOPPANO;

var map = {
    PanoID: '00000000',
    center: {
        lat: 0,
        lng: 30
    },
    zoom: 70,
    canvas: 'container',
    isfbShare: true,
    isState: true,
    showObj: false
};

test.initMap(map);
